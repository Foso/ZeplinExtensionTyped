import Color from "./Color";

export default interface SpacingToken {
    /** Name of the spacing token. */
    name: string

    /** Value of the spacing token. */
    value : String

    /** Color of the spacing token. */
    color : Color

}
